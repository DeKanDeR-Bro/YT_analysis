# YouTube Data Collector (Minecraft War Analysis)

Скрипт для автоматизированного сбора данных с YouTube каналов, участвующих в анализируемом событии (Minecraft War). Собирает информацию о каналах, видео и комментариях за определенный период и сохраняет всё в локальную базу данных SQLite.

## 📋 Возможности

*   **Сбор информации о каналах:** Подписчики, просмотры, количество видео.
*   **Сбор видео:** Загрузка списка видео за указанный период времени.
*   **Сбор комментариев:** Выгрузка комментариев под каждым видео (до 10,000 на видео).
*   **Умные лимиты:** Фильтрация по датам и ограничение количества запросов.
*   **SQLite База данных:** Автоматическое создание и структурирование базы данных (`minecraft_war.db`).
*   **Группировка целей:** Поддержка тегов для каналов (FT, SKY, LITE и т.д.) для удобной аналитики.

## 🛠 Требования

*   Python 3.8+
*   Google YouTube Data API v3 Key

## ⚙️ Установка

1.  **Клонируйте репозиторий** или скачайте файлы в папку проекта.
2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
3.  **Настройте переменные окружения:**
    Создайте файл `.env` в корневой папке проекта и добавьте ваш API ключ YouTube:
    ```env
    YOUTUBE_API_KEY=ваш_ключ_здесь
    ```
    *Чтобы получить ключ, перейдите в [Google Cloud Console](https://console.cloud.google.com/), создайте проект, включите "YouTube Data API v3" и создайте Credentials (API Key).*

## 🚀 Использование

Запустите скрипт командой:

```bash
python collector_final.py
```

**⚠️ Внимание:** При каждом запуске скрипт **полностью удаляет** старую базу данных `minecraft_war.db` и создает новую. Убедитесь, что файл базы данных не открыт в других программах (например, DB Browser for SQLite), иначе скрипт не сможет его перезаписать.

## 📝 Конфигурация (внутри `collector_final.py`)

Вы можете изменять настройки прямо в начале файла скрипта:

*   **Период сбора:**
    ```python
    START_DATE = datetime(2024, 1, 1)
    END_DATE = datetime(2026, 2, 1)
    ```
*   **Лимит комментариев:** `LIMIT_FULL = 10000` (количество комментариев на одно видео).
*   **Список целей (`TARGETS`):** Добавляйте или удаляйте каналы в списке словарей:
    ```python
    {'url': 'ссылка_на_канал', 'tag': 'ВАШ_ТЕГ'}
    ```

## 🗄 Структура Базы Данных

Скрипт создает файл `minecraft_war.db` со следующими таблицами:

### 1. `channels` (Каналы)
| Поле | Тип | Описание |
|---|---|---|
| `channel_id` | TEXT | Уникальный ID канала (PK) |
| `title` | TEXT | Название канала |
| `category` | TEXT | Тег группы (FT, LITE, SKY...) |
| `subscriber_count` | INTEGER | Количество подписчиков |
| `view_count` | INTEGER | Общее количество просмотров |
| `video_count` | INTEGER | Количество видео |

### 2. `videos` (Видео)
| Поле | Тип | Описание |
|---|---|---|
| `video_id` | TEXT | Уникальный ID видео (PK) |
| `channel_id` | TEXT | Ссылка на канал (FK) |
| `title` | TEXT | Название видео |
| `published_at` | TEXT | Дата публикации (ISO) |
| `view_count` | INTEGER | Просмотры |
| `like_count` | INTEGER | Лайки |
| `comment_count` | INTEGER | Количество комментариев (статистика) |
| `duration` | TEXT | Длительность (ISO 8601) |

### 3. `comments` (Комментарии)
| Поле | Тип | Описание |
|---|---|---|
| `comment_id` | TEXT | Уникальный ID комментария (PK) |
| `video_id` | TEXT | Ссылка на видео (FK) |
| `author_name` | TEXT | Имя автора комментария |
| `text` | TEXT | Текст комментария |
| `published_at` | TEXT | Дата публикации |
| `like_count` | INTEGER | Лайки на комментарии |
